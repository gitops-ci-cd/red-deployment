---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: red-route
spec:
  parentRefs:
    - name: http
      namespace: traefik
    - name: red
      kind: Service
      group: core
      port: 80
  hostnames:
    - "red.localhost"
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: "/"
      backendRefs:
        - name: red
          port: 80
